# Zentrix 个人索引

这是一个 Vue/Vite 个人索引站，围绕两条线索组织：**运维/AIOps 项目** 和 **我自己写的 Vue 项目**。首页给出精简概览，可分别进入「项目」和「Vue 项目」两个板块。

生产环境由 Node.js 服务同时提供 Vue 构建产物和后端 API，用户、登录状态与成就进度存储在 PostgreSQL；Kubernetes 采用单常驻副本、发布时临时扩容一个 Pod 的滚动更新策略。

## 当前内容

- **项目**：AIOps MCP Analyzer、OpsAgentAI、CI/CD 流水线实践。
- **Vue 项目**（`/vue-apps`）：把自己写的小页面/小工具收拢成卡片索引，点卡片直接进入：
  - AIOps 智能运维控制台（`/aiops`）：告警筛选、根因分析、MCP 证据与 AI 助手演示。
  - 400 米间歇训练（`/interval-training`）：配速趋势图、评级、日历与导入导出。
  - 人生倒计时（`/countdown`）：按生日与性别估算 35 岁斩杀线、退休与预期寿命。
  - 疯狂的人（`/crazy-people`）：密闭空间发疯小人全自动演示，可当「上帝之手」制造混乱。
  - 世界杯点球大战（`/worldcup`）：拖拽调整角度力度，挑战 AI 守门员的点球游戏。
  - 江阴保卫战形势图（`/jiangyin`）：清军与义军交战路线互动地图，点箭头看事件。
  - 多米诺骨牌（`/domino`）：画一条路线生成骨牌，点推倒看连锁波沿曲线倒下。
  - 炉石传说成就查看器（`/hearthstone`）：按扩展包或职业浏览炉石成就，支持职业折叠总览、筛选、攻略展示、关联卡牌原画。点击推荐卡组名弹出卡组详情（旅法师营地风格：费用宝石 / 类型图标 / 中文牌名 / 卡牌原画缩略图 / 数量，两列网格），可查看完整中文牌表与一键复制卡组码。内置注册登录与「我的成就」模式，可保存个人完成进度、按版本/职业分组、筛选完成状态、统计完成度，并在统一的「冲刺推荐」中按剩余投入分档查看目标；进度更新会写入服务端操作日志。并提供实验性 AI 成就建议（密钥仅服务端使用，前端不接触）与硬核模式（覆盖全部版本）。
- **关于**：说明站点定位和联系方式。

## 更新说明

### 2026-07-24

- 修复生产环境 `/hearthstone-cards/*` 反向代理 404：CI/CD 现在会把 `OSS_ORIGIN` 作为 Docker build-arg 传入并写入容器环境变量（`deploy.yml` 中直接复用已有的 `VITE_OSS_BASE` secret 值，无需新建 secret），服务端代理据此从 OSS 拉取图片；此前仅前端 `VITE_OSS_BASE` 被传入，导致关联卡图等走相对路径的请求全部 404。

### 2026-07-23

- 修正约 30 项成就的「累计 / 一次性」分类与度量（跨 12 个扩展包），按游戏内判定统一；贫瘠之地 9 项改累计-次数、偶一为之 / 有人见过曼科里克的妻子吗？ 改一次性，暗月马戏团 一定有诈、通灵学园 雷象不会遗忘 改累计-次数，多个累计成就显式标注 metric:"count"。
- 职业内成就顺序对齐游戏内：泰坦诸神、传奇音乐节、巫妖王、纳斯利亚堡、奥特兰克、暴风城、沉没之城共 19 组按职业内顺序排列。
- 按版本浏览 / 我的-按版本 分组优化：通灵学院、外域的灰烬、巨龙降临、奥丹姆骑兵、暗影崛起 全部成就归入「光辉事迹」单一分组；核心三年由强制归「中立」改为合并为「职业」（所有职业归一个大栏）+「中立」两大栏，不拆具体职业。
- 关联卡牌修正：巫妖王「你的血，行！」关联卡牌补充为「鲜血远征军」。
- 「我的成就」界面重构：按版本 / 按职业 / 待完成清单 三个子切换下方统一出现版本与职业选择；导出 Excel、批量完成、硬核模式、攻略地址合并为同一操作行；展开全部 / 收起全部、AI 建议、回到顶部统一为风格一致的悬浮按钮（AI 与回到顶部分离不再重叠）。
- 修复进度保存后「待完成清单」统计数字不刷新的问题：保存成功后立即乐观合并到本地状态并强制刷新服务端数据（作废飞行中的旧请求 + no-store），确保「还差 N 次 / N 点」实时更新。

### 2026-07-22

- 双职业成就拆分显示：原先归并到「双职业」分组的成就按真实职业分别展示，浏览与个人视图均不再出现「双职业」聚合项。
- 新增实验性「AI 成就建议」：根据你未完成的成就由服务端调用 DeepSeek 推荐下一步该刷哪一块；弹窗支持 Markdown 渲染、每日限额（5 次固定 + 1 次自由）、透明展示作用域（核心 9 / 全部 27 版本）；需登录后使用。
- 新增「硬核模式」：开启后 AI 建议与统计覆盖全部 27 个版本（含无经验值的 added 版本），默认仅核心 9 个有经验版本。
- 「我的成就 - 按职业」视图下，版本分组按剩余未完成数从低到高排序（剩余越少的版本越靠前，与待完成清单一致）。
- 更新日志页面移至 `/hearthstone/changelog`，作为炉石成就查看器的专属更新说明。

### 2026-07-21

- 导出 PDF/Excel 改为亮色主题：通过 `@media print` 强制明亮配色，不再导出暗色页面。
- 移除成就页「更多攻略」Wiki 下拉入口（使用频率低）。
- 更多版本（addedExpansions）的成就不再进入「按职业浏览 / 我的-按职业 / 冲刺推荐」，仅在按版本浏览中保留。
- 按职业浏览顶部新增醒目横幅：显示当前职业「还剩 X 个未完成（共 Y 个）」，并随指标筛选联动展示剩余次数 / 剩余点数总计。
- 累计型成就细分为「次数 / 点数」两类，并作为可筛选指标（一次性 / 累计-次数 / 累计-点数）；冲刺推荐明确提示「剩余 X 次 / 剩余 X 点」。
- 登录页由暗色背景统一改为亮色主题，与主体风格一致。
- 按职业视图默认展开全部职业（用户觉得太长可点「收起全部」折叠）。
- 按版本视图重排布局：进度条 → 剩余统计（剩余成就/次数/点数）→ 共 X 个成就 + 版本链接 → 搜索 → 导出 / 批量完成；并修复完成度在「我的-按版本」中重复显示的问题。
- 成就卡片徽标统一为 4 个椭圆：版本 / 职业 / 类型(一次性·点数·次数) / 难度；修复暗色样式作用域丢失导致「次数」字样不显示的空椭圆问题。
- 我的成就页「导出 Excel / 批量完成」工具条由硬编码暗色改为主题变量，明暗主题自适应。

### 2026-07-23

- 卡组详情卡牌图统一走阿里云 OSS：前端用相对路径 `/hearthstone-cards/...`（以本站域名开头，域名无需在阿里云备案），服务端反向代理到 OSS 并强制 `Content-Disposition: inline`（右键新标签直接查看不下载）；按旅法师营地风格展示法力水晶 / 稀有度着色牌名 / 缩略图 / 数量，点击卡牌弹灯箱看大图。关联成就卡图仅走 OSS（未配图源时显示「暂无图」）。
- 卡牌元数据 `data/dbfid-cardnames.json` 重建为全量卡牌库（21451 张，含衍生 / 变身 token 卡），补全卡组码解码后曾显示为 `#dbfId` 的卡；费用 / 类型 / 稀有度均取自本地库，不联网。
- 修复白卡文字在弹窗深色背景下不可见的问题，改为跟随主题（亮色黑、暗色白）。
- 修正 5 个成就职业归属：语声鼎沸→法师、污手马仔→圣骑士、法力洼地→萨满祭司、食量极限→恶魔猎手、都缝好了→死亡骑士。
- 顶部「我的成就」吸顶块重组：子切换 + 版本/职业选择 + 操作行合并为整体滚动固定；版本名/职业名改为金色胶囊芯片显示在子切换左侧。
- 「展开全部 / 收起全部」统一移到版本/职业选择之后同一行，在按版本浏览、我的-按版本、我的-按职业三处位置与触发条件一致。
- 卡组弹窗背景与文字跟随主页亮/暗主题（亮色弹窗亮色、白卡文字黑色；暗色弹窗暗色、白卡文字白色）。

### 2026-07-21（续）

- 新增卡组详情弹窗：点击成就卡片里的推荐卡组名（不再误触卡片整体）弹出，显示「职业 · 共 N 张」、可复制卡组码，并支持卡组介绍（如「无差别骑士」的瓦玛骑草稿）。
- 卡组详情改旅法师营地风格：每张卡牌显示费用宝石（按职业着色）、类型图标（随从 / 法术 / 武器）、中文牌名、HearthstoneCDN 卡牌原画缩略图与数量，两列网格布局。
- 新增卡组码解码器 `utils/deckstring.js`：修复 varint 读取偏移（从索引 2 跳过 2 字节头），并以全部 87 个推荐卡组码反推出真实英雄 dbfId→职业映射，含严格边界校验（各段 ≤40、总张数 1~40）。
- 新增卡牌名映射 `data/dbfid-cardnames.json`：基于 HearthstoneJSON zhCN 数据库（8107 张可收集卡），含 name / cost / type / cardClass / rarity / id，卡组解码可输出完整中文牌表与原画图 URL。
- 服务端日志 `server/logger.js` 改为北京时间（+08:00）输出，不再使用 UTC 的 `toISOString()`。
- 修订 12 条成就的关联卡牌：时间酷刑→续连熵能 / 暮光时空跃迁者 / 超光子弹幕；尸死无生→残骸遍野；好人坏人食人 + 帮派头领普德；嗷嗷待哺→待哺群雏 / 啸天龙蛋；伶俐猛龙→盛宴之角；忍者始祖龟→审讯；烫手山芋→焚火林地 / 焚火之力 / 焚火飞升；Ctrl+Z / 传世奇闻 / 准备，瞄准…… / 挖呀挖呀挖 / 通缉：不论死活 / 连环灾异 清空关联卡牌。
- 桌面炉石卡牌图片归位：50+ 张 PNG 按 `relatedCards` 精确名复制到 `assets/cards/<cardImageDir>/`；修正「观星（决战荒芜之地）」带后缀的匹配问题。

### 2026-07-20

- 使用 UI/UX Pro Max 重构炉石子项目界面，统一为深色炉石风格，并优化首页概览、筛选器、成就卡片、登录注册页、进度编辑弹窗和移动端布局。
- 合并「快完成」与「推荐冲刺」为「冲刺推荐」，按 A～E 五档展示目标；版本、职业筛选会统一作用于列表和数量统计，避免有时显示全部、有时只显示选中项。
- 完善注册与登录异常处理，后端不可用或返回空响应时显示明确提示，避免前端出现 JSON 解析错误。
- 新增不依赖 PostgreSQL 的本地验证模式，账号和进度仅保存在当前进程内，方便快速测试注册、登录和进度更新。
- 注册、登录、登出和成就进度更新继续写入按日期拆分的应用日志 `logs/app-YYYY-MM-DD.log`；容器内默认位置为 `/app/logs/`。
- Kubernetes 部署改为 `RollingUpdate`，发布期间最多临时增加一个 Pod，并补充启动、就绪、存活探针和优雅停机，减少单常驻副本更新时的短暂 502。
- 新增「批量勾选完成成就」功能：我的成就页可勾选多个成就后一次性标记完成，支持全选当前范围 / 清除已选。
- 补充更多扩展包成就数据：导入 wiki 18 个版本的本地化成就（alterac、barrens、core-2021/2022/2023、darkmoon、dragons、legend-festival、lich-king、nathria、outland、rise-of-shadows、scholomance、stormwind、sunken-city、titan、uldum、zonghe 等）。
- README 新增「联系 / 作者」小节：GitHub @zentrix566、Email zentrix566@gmail.com。

## 运行方式

安装依赖：

```bash
npm install
```

同时启动前端开发服务器和后端服务：

```bash
npm run dev
```

默认访问 Vite 输出的本地地址。

不连接 PostgreSQL，使用进程内存测试注册、登录和成就进度：

```bash
npm run dev:local
```

本地模式没有预置测试账号，可直接在注册页面创建账号；服务停止后账号和进度会清空。

## 常用命令

构建生产环境前端产物：

```bash
npm run build
```

本地预览构建产物：

```bash
npm run preview
```

查看当天的注册、登录和进度更新日志：

```bash
Get-Content -Wait logs/app-$(Get-Date -Format yyyy-MM-dd).log
```

## Docker / Nginx

```bash
docker build -t my-index .
docker run -d -p 8080:80 my-index
```

访问 http://localhost:8080。

## 目录结构

```text
.
├── src/
│   ├── components/       # 复用组件（项目卡片网格、Vue 项目卡片网格）
│   ├── crazy-people/     # 「疯狂的人」小游戏（组件/游戏逻辑/主循环）
│   ├── hearthstone-achievements/  # 炉石传说成就查看器（组件/数据/卡牌图片）
│   ├── data/             # 项目、训练与 Vue 项目索引数据
│   ├── router/           # Vue Router
│   ├── styles/           # 全局样式
│   ├── views/            # 页面
│   ├── App.vue
│   └── main.js
├── index.html            # Vite 入口
├── vite.config.js
├── nginx.conf            # Nginx 配置
├── Dockerfile            # 多阶段构建镜像
└── README.md
```

## 联系 / 作者

- GitHub: [@zentrix566](https://github.com/zentrix566)

## 许可证

本项目基于 [MIT 许可证](LICENSE) 开源。
